# Dependencies
node_modules/

# Build artifacts (regenerated by `npm run build`)
# build/ is committed for distribution; uncomment to ignore during development.

# Packaged plugin (ignored everywhere…)
*.zip
# …except the published preview build served via GitHub Pages.
!/woocommerce-quote.zip

# OS / editor noise
.DS_Store
Thumbs.db
*.log
.idea/
.vscode/
